/**
 * Temporary feature flags.
 *
 * Flip these back to `true` to bring the features back — nothing about the
 * underlying Boost Reach / Occa Corporate (Account Boost & Membership)
 * functionality has been removed, it's just hidden from the UI for now.
 */

/** "Boost Reach" / "Extend Boost" on individual listings (MyPostsPage). */
export const BOOSTS_ENABLED = false;

/** "Occa Corporate" — Account Boost & Occa Membership upgrade plans (Settings + CorporateConsoleSheet). */
export const CORPORATE_PLANS_ENABLED = false;
